<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:name=".FamilyConnectApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.FamilyConnect">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:windowSoftInputMode="adjustResize"
            android:keepScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground service for listening to calls and messages in background -->
        <service
            android:name=".notifications.CallListenerService"
            android:exported="false"
            android:foregroundServiceType="phoneCall" />

        <!-- Foreground service for displaying incoming call UI -->
        <service
            android:name=".notifications.CallForegroundService"
            android:exported="false"
            android:foregroundServiceType="phoneCall" />

        <!-- Firebase Cloud Messaging service -->
        <service
            android:name=".notifications.FCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Broadcast receiver for call accept/reject from notification -->
        <receiver
            android:name=".notifications.CallActionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.familyconnect.app.ACTION_ACCEPT_CALL" />
                <action android:name="com.familyconnect.app.ACTION_REJECT_CALL" />
            </intent-filter>
        </receiver>

        <!-- Restart service after device reboot -->
        <receiver
            android:name=".notifications.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Dedicated incoming call activity for showing full call UI immediately -->
        <activity
            android:name=".IncomingCallActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:windowSoftInputMode="adjustResize"
            android:keepScreenOn="true" />

        <!-- Full-screen alarm activity for reminder notifications -->
        <activity
            android:name=".notifications.ReminderAlarmActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:windowSoftInputMode="adjustResize"
            android:keepScreenOn="true" />

        <!-- Broadcast receiver for reminder snooze timer expiration -->
        <receiver
            android:name=".notifications.ReminderAlarmReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.familyconnect.app.SNOOZE_EXPIRED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
